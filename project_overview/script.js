document.addEventListener('DOMContentLoaded',()=>{initScrollProgress();initNavHighlight();initSmoothScroll();initThemeToggle();initBackToTop();initCopyCode()});
function initScrollProgress(){const b=document.getElementById('scroll-progress');window.addEventListener('scroll',()=>{const s=(scrollY/(document.documentElement.scrollHeight-innerHeight))*100;b.style.width=s+'%'})}
function initNavHighlight(){const s=document.querySelectorAll('.section,.hero'),l=document.querySelectorAll('.nav-link');window.addEventListener('scroll',()=>{let c='';s.forEach(e=>{if(scrollY>=e.offsetTop-100)c=e.id});l.forEach(a=>{a.classList.remove('active');if(a.getAttribute('href')==='#'+c)a.classList.add('active')})})}
function initSmoothScroll(){document.querySelectorAll('a[href^="#"]').forEach(a=>{a.addEventListener('click',e=>{e.preventDefault();const t=document.querySelector(a.getAttribute('href'));if(t)scrollTo({top:t.offsetTop-60,behavior:'smooth'})})})}
function initThemeToggle(){const t=document.getElementById('theme-toggle');const s=localStorage.getItem('theme')||'dark';document.documentElement.setAttribute('data-theme',s);t.textContent=s==='dark'?'🌙':'☀️';t.addEventListener('click',()=>{const c=document.documentElement.getAttribute('data-theme');const n=c==='dark'?'light':'dark';document.documentElement.setAttribute('data-theme',n);localStorage.setItem('theme',n);t.textContent=n==='dark'?'🌙':'☀️'})}
function initBackToTop(){const b=document.getElementById('back-to-top');window.addEventListener('scroll',()=>{if(scrollY>600)b.classList.add('visible');else b.classList.remove('visible')});b.addEventListener('click',()=>scrollTo({top:0,behavior:'smooth'}))}
function initCopyCode(){document.querySelectorAll('.copy-btn').forEach(b=>{b.addEventListener('click',()=>copyCode(b))})}
function copyCode(b){const c=b.closest('.code-block').querySelector('code');if(c){navigator.clipboard.writeText(c.textContent).then(()=>{b.textContent='已复制';setTimeout(()=>b.textContent='复制',2e3)})}}
