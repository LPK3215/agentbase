# 10 改进闭环跟踪表

版本：`acceptance-2026-09-07-v1`  
尺子锁定：agent-acceptance v1.2.0；本表整改期间 **不改判据、不改合格线**。  
责任角色默认：仓库维护者（被检方）。复评角色：持本技能的检查者。  
期限为建议；被检方可在复评前改日期，但 P0 不清零则结论档不升。

脚手架型在 2.7 / 2.12.5 / 2.12.7 / 7.5 上的 P0/P1 **必须写附录 B 编号**。

回归格式（整改后填）：`整改前判点 → 整改证据（路径）→ 重跑结果`。安全类须回到 7.5。

---

## P0 — 门槛 / 否决相关（未清零不讨论升档）

本轮无行为红线触顶，P0 只有「7.5.1 门槛件」与生产误用防护。

| ID | 项 | 对应判点 | 附录 B | 动作 | 期限建议 | 重跑 | 状态 |
|---|---|---|---|---|---|---|---|
| P0-1 | 书面 fail-closed 红线清单 | 7.5.1 | **B.1** | 新增 `SECURITY.md` 或 `docs/security.md`，按 B.1 六行表声明 R1–R6 的检测方式与触发后行为；注明「脚手架由使用者按场景启用」 | 下一次发布前 | 重跑 7.5.1：有表即本项达标，章 7 可从「不达标」改「警示/达标（门槛件已过）」 | 开放 |
| P0-2 | 生产鉴权必开（防误用） | 7.1.1 | （B.1 R1 联动） | 在 README / SECURITY 醒目位置写：未设 `AGENTBASE_API_KEY` 且非 jwt 时 API **开放**；生产必须设密钥或 jwt secret。可选：`app.env=prod` 时拒绝无鉴权启动（实现属「改」，需被检方确认） | 与 P0-1 同批 | 文档复核；若做 fail-closed 启动则加测试 | 开放 |

P0-2 若只改文档、不改默认行为：符合脚手架「默认关 = 警示」。**不要在未确认的情况下改产品默认值。**

---

## P1 — 影响结论表述 / 接入位置升级

| ID | 项 | 对应判点 | 附录 B | 动作 | 期限建议 | 重跑 | 状态 |
|---|---|---|---|---|---|---|---|
| P1-1 | HITL 触发点总表 + 默发策略说明 | 7.4.1 / 7.2.4 / 2.12.5 | **B.2** | 在 `docs/guardrails.md` 或 README 扩展指南：列出写/删/外发/代码执行哪些要 interrupt；说明默发 `interrupt_on: {}` 的风险；可保留 interrupt_demo 作示例。2.12.5 已是二级，本项目标是 7.4 降警示、避免应用方误把默当安全 | 两周内文档 | 重跑 7.4.1、2.12.5 | 开放 |
| P1-2 | 评估挂钩进 CI 的文档与接线 | 2.12.7 | **B.3** | 按 B.3 写评估扩展指南（入口、exit 1、阈值）。实现上对接 ROADMAP **N2**（`agentbase eval --suite examples/eval_suite.yaml` 进 CI）。套件需先改掉名不副实的 `refusal_boundary` | 与 N2 同批 | 重跑 2.12.7；CI 日志作 E1 | 开放 |
| P1-3 | 反思回路接入文档 | 2.7 | **B.4** | 把现有 middleware/失败日志文档化成 onFailure / 经验沉淀 / reflectionTriggers 伪码示例。不强制做自动反思实现（G1） | 两周内文档 | 重跑 2.7：有文档化示例则升二级达标（接入位置级） | 开放 |
| P1-4 | 安全测试框架扩展位 | 7.5.2–7.5.6 | **B.5** | 建 `tests/security/` 目录骨架 + redlines.yaml 引用 B.1；至少各 1 条注入/越权占位。不要求本轮红队 | 与 P0-1 同批或紧随 | 重跑 7.5.2：有扩展位置文档+目录即可从「完全无」升「警示偏达标（脚手架）」 | 开放 |
| P1-5 | 默发写/删工具风险 | 7.2.4 / 1.6 | B.2 | 文档声明：default agent 含 write_file 与多种 delete，且无 interrupt。可选提供 `readonly` 画像 YAML（只读工具集） | 与 P1-1 同批 | 文档复核 | 开放 |
| P1-6 | 审计 / 脱敏启用说明 | 7.6.4 / 7.6.6 / 2.2.6 | B.1 R3/R5 | 文档写清 `audit.enabled` / `redaction.enabled` / `redact_output` 中间件如何打开；生产建议组合 | 与 P0-1 同批 | 文档复核 | 开放 |
| P1-7 | `/metrics` 公网暴露 | 2.12.6 | — | 文档：`/metrics` 在 `_PUBLIC_PATHS` 无鉴权。生产应用网关限制 | 随 P0-2 | 文档复核 | 开放 |

---

## P2 — 可让步 / 路线图已有

| ID | 项 | 对应 | 动作 | 状态 |
|---|---|---|---|---|
| P2-1 | 轨迹 grader | ROADMAP N1 | 接入 EvaluationRunner | 已在路线图，P2 |
| P2-2 | 自动记忆 save/recall | ROADMAP N3 | 默认关 | 已在路线图 |
| P2-3 | 输出 guardrail | ROADMAP N4 / 7.3.3 | 禁词/schema/abstention，默认关 | 已在路线图 |
| P2-4 | 租户模型 | 7.1.7 / 7.6.5 | 非零配置目标；要多租户再做 tenant_id 强制注入 | 开放 |
| P2-5 | 更强代码沙箱 | 7.2.5 | 当前 subprocess ≠ jail；文档降级声称或换隔离 | 开放 |
| P2-6 | 会话 TTL | 2.10.4 | 生产设 `session_ttl_seconds` | 开放 |
| P2-7 | 换模型复测单 | 2.1.4 | 与 B.3 套件绑定 | 开放 |
| P2-8 | 用量配额熔断 | 2.1.5 / 2.12.8 | usage 默认关；要成本闸再开 | 开放 |
| P2-9 | `.github/` gitignore | U-CI | 授权 workflow scope 后纳入版本库，避免「本地有 CI、远程无」 | 开放 |
| P2-10 | untrusted 内容标记 | 2.2.3 / 7.3.1 | 工具/RAG 结果加不可覆盖系统指令的约束 | 开放 |
| P2-11 | 写工具幂等契约 | 2.10.2 | 重试不双写 | 开放 |

P2 **不阻塞** 7.5.1 复评后的章结论变化。

---

## 回归与版本纪律（10.3.2–10.3.3）

1. P0-1 完成后：只重跑 7.5.1 与第 10 章合成，**不重写第 2 章尺子**。
2. 安全整改必须带至少 1 条 B.5 样本或「模板已落盘」证据，禁止只报「加了 SECURITY.md」而无表内容。
3. 复验更新：改写同名文件，结论行注明复验日期；本版移 `acceptance/agentbase/2026-09-07/archive/`。
4. 退出本轮闭环：P0-1 清零 + 支柱项补证或显式保持未评估。即使 P0 清零，中度仍封顶原型，除非升级检查级别。

---

## 建议排期（不替被检方做产品决策）

```
P0-1 B.1 SECURITY.md     ──┐
P0-2 鉴权误用声明        ──┼─ 可同一 PR（纯文档）
P1-6 审计脱敏说明        ──┘
P1-1/P1-5 B.2 护栏文档
P1-3 B.4 反思文档
P1-4 B.5 目录骨架
P1-2/N2 eval CI           （含实现，需单独排）
N1/N3/N4                  （路线图原优先级）
```

按项目工作模式 **改 > 删 > 查 > 增**：上述 P0/P1 以**文档与接入位置**为主，不要求本轮新造安全产品功能。若做 `prod 拒绝无鉴权启动`，属「改」且改变默认行为，须维护者确认后再动代码。
